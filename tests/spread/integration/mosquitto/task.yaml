summary: Integration tests for mosquitto

execute: |
  # Chisel a minimum number of slices to give us a runnable system that we can
  # test in.
  rootfs="$(install-slices mosquitto_bins)"

  chroot "${rootfs}/" mosquitto -h | head -1
